PORT=9865

DB_HOST=localhost
USER_ROOT=root
DB_PASSWORD=
DB_NAME=MySQL_SYS_Database

JWT_SESSION_SECRET=replace-with-a-long-random-secret
SESSION_TTL_SECONDS=43200
JWT_ISSUER=field-outreach-api
JWT_AUDIENCE=field-outreach-pwa

APP_PUBLIC_URL=http://localhost:5173
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Field Outreach <no-reply@field-outreach.local>

# Cloudinary Configuration (Optional - Falls back to Base64 DB storage if empty)
CLOUDINARY_URL=

